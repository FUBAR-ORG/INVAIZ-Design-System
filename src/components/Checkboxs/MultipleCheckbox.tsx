export interface CheckboxProps {
  checked?: boolean;
  onChange?: (checked: boolean) => void;
}
const MultipleCheckbox = ({ checked, onChange }: CheckboxProps) => {
  return <></>;
};

export default MultipleCheckbox;
