import { render } from '@tests/test-utils';
// test utils

// components

describe('MultipleContentsTooltip', () => {
  it('다수 콘텐츠 툴팁은 적용된 요소들에게 기본적으로 영향을 주지 않는다.', () => {});

  it('다수 콘텐츠 툴팁이 적용된 요소 각각에 `mouseover` 시 다수 콘텐츠 툴팁이 각 요소의 주변에 렌더링된다.', () => {});

  it('다수 콘텐츠 툴팁 컴포넌트에 `mouseleave` 후 일정 시간이 지나면 사라진다.', () => {});

  it('다수 콘텐츠 툴팁 `mouseleave` 후 사라지기 전에 `mouseover` 시 정상적으로 렌더링 상태를 유지한다.', () => {});

  it('다수 콘텐츠 툴팁 중 한 가지 요소에서 `mouseleave` 후 사라지기 전에 다른 요소에 `mouseover`를 하면 새로운 다수 콘텐츠 툴팁이 해당 요소의 주변에 렌더링된다.', () => {});
});
