import { render } from '@tests/test-utils';
// test utils

// components

describe('HaveIconTooltip', () => {
  it('아이콘 포함 툴팁은 적용된 요소에 기본적으로 영향을 주지 않는다.', () => {});

  it('적용된 요소에 `mouseover` 시 아이콘 포함 툴팁이 렌더링된다.', () => {});

  it('아이콘 포함 툴팁 컴포넌트에 `mouseleave` 후 일정 시간이 지나면 사라진다.', () => {});

  it('아이콘 포함 툴팁 `mouseleave` 후 사라지기 전에 `mouseover` 시 정상적으로 렌더링 상태를 유지한다.', () => {});
});
